/*
read assertion
determine if assertion is JSON-LD - compact function?
if JSON-LD - get context definition links
use context definition to get validation property (schema)
if not JSON-lD - use lib schema

validate against schema
Note additional properties that can't be validated and return warning

If JSON-LD - allow export options
-c --compact
-e --expand
-f --flatten
-fr --frame
-nq --nquads
-n --normalize

*/
